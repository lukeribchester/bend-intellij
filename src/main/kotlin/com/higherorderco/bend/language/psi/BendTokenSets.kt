package com.higherorderco.bend.language.psi

import com.intellij.psi.tree.TokenSet


interface BendTokenSets {
    companion object {
        /* Comments */
//        val COMMENTS: TokenSet = TokenSet.create(BendTypes.COMMENT)

        /* Literals */
//        val STRINGS: TokenSet = TokenSet.create(BendTypes.STR)
    }
}
